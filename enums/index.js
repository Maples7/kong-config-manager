module.exports = {
  OBJECTS: ['apis', 'consumers', 'plugins', 'certificates', 'snis', 'upstreams'],
  IDENTIFIRES: {
    apis: 'id',
    consumers: 'id',
    plugins: 'id',
    certificates: 'id',
    snis: 'name',
    upstreams: 'id',
    targets: 'id'
  }
};
